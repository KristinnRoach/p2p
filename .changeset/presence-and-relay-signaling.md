---
"@kidlib/p2p": patch
---

Add per-member presence data so apps can attach roster metadata (e.g. display name, mute state) to room members and update it mid-call, and add `createRelayPeerSignaling()` for adapting peer-addressed relay transports into pair signaling.
